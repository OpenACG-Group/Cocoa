<?xml version="1.0" encoding="UTF-8"?>
<internal>
    <directive>
        <scope attr="UserExecute" value="forbidden"/>
        <scope attr="UserImport" value="forbidden"/>
        <scope attr="SysExecute" value="allowed"/>
        <scope attr="SysImport" value="forbidden"/>
        <author>OpenACG Group(R) Cocoa Project</author>
    </directive>
    <content>
        class RefValue {
            constructor(value) {
                this.value_ = value;
                this.locked_ = false;
            }

            get() {
                return this.value_;
            }

            set(value) {
                if (this.locked_)
                    throw Error("Operation not permitted");
                this.value_ = value;
            }

            lock() {
                this.locked_ = true;
            }

            unlock() {
                this.locked_ = false;
            }

            isLocked() {
                return this.locked_;
            }

            clone() {
                let ref = new RefValue(this.value_);
                ref.locked_ = this.locked_;
                return ref;
            }

            static __has_instance__(value) {
                return value instanceof RefValue;
            }
        }
        global.RefValue = RefValue;
    </content>
</internal>
