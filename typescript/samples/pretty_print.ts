import { print } from '../core/pretty_printer';

let object = {
    numberValue: 100,
    floatNumberValue: 3.1415,
    stringValue: 'Hello, World',
    booleanValue: true,
    booleanValue2: false,
    arrayValue: [1, 2 ,3 ,4]
}

let core = await import('core');

